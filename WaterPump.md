# WaterPump

`WaterPump` 是注水口的控制器类。
挂载有此类组件的对象在启用时可以使水体上升到至少与自身齐平的高度。

- 继承自：[`Waterlet`](Waterlet.md)

## 序列化字段

- `ParticleSystem particle`：展示流水的粒子发生器。

	粒子的生存周期会被自动调整为正好接触到水面的长度。

	当水面高于注水口时，不会有粒子生成。

	> 粒子发生器只是暂时的视觉呈现方案，将来会更新。